# TorchTPU

This directory holds examples of using tokamax with TorchTPU.

Current and future work:

* Calling a kernel directly
* Autotuning a kernel directly

* Calling a kernel through a PyTorch custom op wrapper
* Autotuning a kernel through a TorchTPU wrapper 