# README

This directory holds an experimental shim to package up tokamax ops as PyTorch
custom ops compatible with the TorchTPU backend. 

This API design is intended to be compatible with the CUDA backend as well.